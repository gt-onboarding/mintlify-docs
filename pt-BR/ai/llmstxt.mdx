---
title: "llms.txt"
description: "Facilite a leitura e a indexação do seu conteúdo por LLMs"
icon: "file-code"
---

import { PreviewButton } from "/snippets/previewbutton.jsx"

O arquivo [llms.txt](https://llmstxt.org) é um padrão do setor que ajuda LLMs a indexar conteúdo de forma mais eficiente, semelhante a como um sitemap ajuda os mecanismos de busca. Ferramentas de IA podem usar esse arquivo para entender a estrutura da sua documentação e encontrar conteúdo relevante para as consultas dos usuários.

A Mintlify hospeda automaticamente um arquivo `llms.txt` na raiz do seu projeto que lista todas as páginas disponíveis na sua documentação. Esse arquivo está sempre atualizado e não requer manutenção. Você também pode adicionar um arquivo `llms.txt` personalizado à raiz do seu projeto.

Visualize seu `llms.txt` adicionando `/llms.txt` à URL do seu site de documentação.

<PreviewButton href="https://mintlify.com/docs/llms.txt">Abrir o llms.txt deste site.</PreviewButton>


<div id="llmstxt-structure">
  ## Estrutura do llms.txt
</div>

Um arquivo `llms.txt` é um arquivo Markdown simples que contém:

- **Título do site** como um cabeçalho H1.
- **Seções de conteúdo estruturadas** com links e uma descrição de cada página da sua documentação.

```mdx Example llms.txt
# Example product docs

## Guides
- [Getting started](https://example.com/docs/start): Intro guide
- [Install](https://example.com/docs/install): Setup steps

## Reference
- [API](https://example.com/docs/api): Endpoint list and usage
```

Essa abordagem estruturada permite que LLMs processem sua documentação de forma eficiente em um nível geral e localizem conteúdo relevante para as consultas dos usuários, melhorando a precisão e a velocidade das buscas de documentação assistidas por IA.

<div id="llms-fulltxt">
  ## llms-full.txt
</div>

O arquivo `llms-full.txt` reúne todo o seu site de documentação em um único arquivo como contexto para ferramentas de IA e é indexado pelo tráfego de LLMs.

A Mintlify hospeda automaticamente um `llms-full.txt` na raiz do seu projeto. Visualize seu `llms-full.txt` adicionando `/llms-full.txt` ao URL do seu site de documentação.

<PreviewButton href="https://mintlify.com/docs/llms-full.txt">Abrir o llms-full.txt deste site.</PreviewButton>

<div id="custom-files">
  ## Arquivos personalizados
</div>

Para adicionar um arquivo personalizado `llms.txt` ou `llms-full.txt`, crie um `llms.txt` ou `llms-full.txt` na raiz do seu projeto. Adicionar um arquivo personalizado substituirá o arquivo gerado automaticamente com o mesmo nome. Se você excluir o arquivo personalizado, o arquivo padrão voltará a ser usado.

Seu arquivo personalizado `llms.txt` ou `llms-full.txt` deve incluir o título do site como um cabeçalho H1. O restante do conteúdo é opcional. Consulte [Format](https://llmstxt.org/#format) na especificação do `llms.txt` para mais informações sobre seções opcionais e boas práticas.