---
title: "Redirecionamentos e links quebrados"
description: "Ferramentas para evitar links inválidos"
icon: "link-2"
---

Quando você altera o caminho de um arquivo na pasta docs, o caminho da URL dessa página também é alterado. Isso pode ocorrer ao reestruturar a documentação ou ao mudar o título da barra lateral.

<div id="broken-links">
  ## Links quebrados
</div>

Encontre links quebrados com nossa CLI. [Instale a CLI](/pt-BR/installation) e execute o comando:

```bash
mint broken-links
```

A CLI identifica quaisquer links relativos na sua documentação que não existam.

<div id="redirects">
  ## Redirecionamentos
</div>

Configure redirecionamentos 301 adicionando o campo `redirects` ao arquivo `docs.json`.

```json
"redirects": [
  {
    "source": "/source/path",
    "destination": "/destination/path"
  }
]
```

Isso redireciona permanentemente `/source/path` para `/destination/path`, garantindo que você não perca o SEO acumulado da página original.

Para corresponder a um caminho com curinga, use `*` após um parâmetro. Neste exemplo, `/beta/:slug*` corresponde a `/beta/introduction` e o redireciona para `/v2/introduction`.

```json
"redirects": [
  {
    "source": "/beta/:slug*",
    "destination": "/v2/:slug*"
  }
]
```