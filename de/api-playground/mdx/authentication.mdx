---
title: "Authentifizierung"
description: "Sie können Authentifizierungsparameter festlegen, damit Nutzer ihre realen API-Schlüssel verwenden."
---

<div id="enabling-authentication">
  ## Authentifizierung aktivieren
</div>

Sie können Ihrer `docs.json` eine Authentifizierungsmethode hinzufügen, um sie global auf allen Seiten zu aktivieren, oder sie seitenweise festlegen.

Die Authentifizierungsmethode einer Seite überschreibt die globale Methode, wenn beide definiert sind.

<div id="bearer-token">
  ### Bearer-Token
</div>

<CodeGroup>

```json docs.json
"api": {
    "mdx": {
      "auth": {
        "method": "bearer"
      }
    }
}
```

```mdx Page Metadata
---
title: "Seitentitel"
authMethod: "bearer"
---
```

</CodeGroup>

<div id="basic-authentication">
  ### HTTP-Basic-Authentifizierung
</div>

<CodeGroup>

```json docs.json
"api": {
    "mdx": {
      "auth": {
        "method": "basic"
      }
    }
}
```

```mdx Page Metadata
---
title: "Seitentitel"
authMethod: "basic"
---
```

</CodeGroup>

<div id="api-key">
  ### API-Schlüssel
</div>

<CodeGroup>

```json docs.json
"api": {
    "mdx": {
      "auth": {
        "method": "key",
        "name": "x-api-key"
      }
    }
}
```

```mdx Page Metadata
---
title: "Seitentitel"
authMethod: "key"
---
```

</CodeGroup>

<div id="none">
  ### None
</div>

Die Authentifizierungsmethode „none“ ist hilfreich, um die Authentifizierung für einen bestimmten Endpoint zu deaktivieren, nachdem in der docs.json ein Standard festgelegt wurde.

<CodeGroup>
```mdx Page Metadata
---
title: "Your page title"
authMethod: "none"
---
```
</CodeGroup>