---
title: "CLI-Installation"
description: "Verwenden Sie die CLI, um Ihre Dokumentation lokal in der Vorschau anzusehen und zu verwalten"
---

<img className="block dark:hidden my-0 pointer-events-none" src="/images/installation/local-development-light.png" alt="Dekorative Grafik, die die CLI darstellt." />

<img className="hidden dark:block my-0 pointer-events-none" src="/images/installation/local-development-dark.png" alt="Dekorative Grafik, die die CLI darstellt." />

Verwenden Sie die CLI, um Ihre Dokumentation lokal während des Schreibens und Bearbeitens in der Vorschau anzusehen. Sehen Sie Änderungen in Echtzeit, bevor Sie deployen, testen Sie das Erscheinungsbild und die Funktionalität Ihrer Dokumentationsseite und erkennen Sie Probleme wie defekte Links oder Barrierefreiheitsprobleme.

Die CLI bietet außerdem Tools zur Pflege Ihrer Dokumentation, darunter Befehle zum Umbenennen von Dateien, Validieren von OpenAPI-Spezifikationen und Migrieren von Inhalten zwischen Formaten.

<div id="install-the-cli">
  ## CLI installieren
</div>

<Info>
  **Voraussetzung**: Die CLI erfordert [Node.js](https://nodejs.org/en) v19 oder höher.
</Info>

<Steps>
  <Step title="CLI installieren">
    Führe den folgenden Befehl aus, um die [CLI](https://www.npmjs.com/package/mint) zu installieren:

    <CodeGroup>
      ```bash npm
      npm i -g mint
      ```

      ```bash pnpm
      pnpm add -g mint
      ```
    </CodeGroup>
  </Step>

  <Step title="Lokal prüfen">
    Navigiere zu deinem Docs-Verzeichnis (in dem sich die Datei `docs.json` befindet) und führe den folgenden Befehl aus:

    ```bash
    mint dev
    ```

    Eine lokale Vorschau deiner Dokumentation ist unter `http://localhost:3000` verfügbar.
  </Step>
</Steps>

Alternativ kannst du, wenn du die CLI nicht global installieren möchtest, ein einmaliges Skript ausführen:

```bash
npx mint dev
```

<div id="updates">
  ## Aktualisierungen
</div>

Wenn Ihre lokale Vorschau nicht mit der Produktionsversion im Web übereinstimmt, aktualisieren Sie Ihre lokale CLI:

```bash
mint update
```

Wenn der Befehl `mint update` in Ihrer lokalen Version nicht verfügbar ist, installieren Sie die CLI in der neuesten Version neu:

<CodeGroup>
  ```bash npm
  npm i -g mint@latest
  ```

  ```bash pnpm
  pnpm add -g mint@latest
  ```
</CodeGroup>

<div id="custom-ports">
  ## Benutzerdefinierte Ports
</div>

Standardmäßig verwendet die CLI Port 3000. Sie können den Port mit dem Flag `--port` anpassen. Um die CLI beispielsweise auf Port 3333 auszuführen, verwenden Sie diesen Befehl:

```bash
mint dev --port 3333
```

Wenn Sie versuchen, einen bereits belegten Port zu verwenden, wird der nächste verfügbare Port genutzt:

```mdx
Port 3000 ist bereits belegt. Versuche stattdessen 3001.
```

<div id="preview-as-a-specific-group">
  ## Als bestimmte Gruppe anzeigen
</div>

Wenn Sie Partielle Authentifizierung verwenden, um den Zugriff auf Ihre Dokumentation einzuschränken, können Sie mit dem Flag `--groups [groupname]` eine Vorschau als bestimmte Authentifizierungsgruppe anzeigen.

Wenn Sie beispielsweise eine Gruppe namens `admin` haben, können Sie mit dem folgenden Befehl eine Vorschau als Mitglied dieser Gruppe anzeigen:

```bash
mint dev --groups admin
```

<div id="additional-commands">
  ## Weitere Befehle
</div>

<div id="find-broken-links">
  ### Kaputte Links finden
</div>

Ermitteln Sie defekte interne Links mit dem folgenden Befehl:

```bash
mint broken-links
```

<div id="find-accessibility-issues">
  ### Zugänglichkeitsprobleme finden
</div>

Teste die Farbkontrastverhältnisse und suche nach fehlendem Alt-Text bei Bildern und Videos in deiner Dokumentation mit dem folgenden Befehl:

```bash
mint a11y
```

<div id="check-openapi-spec">
  ### OpenAPI-Spezifikation prüfen
</div>

Prüfen Sie Ihre OpenAPI-Datei auf Fehler mit dem folgenden Befehl:

```bash
mint openapi-check <OpenAPI-Dateiname oder -URL>
```

Geben Sie einen Dateinamen (zum Beispiel `./openapi.yaml`) oder eine URL (zum Beispiel `https://petstore3.swagger.io/api/v3/openapi.json`) an.

<div id="rename-files">
  ### Dateien umbenennen
</div>

Benennen Sie Dateien um und aktualisieren Sie alle Verweise darauf mit dem folgenden Befehl:

```bash
mint rename <pfad/zur/alten-datei> <pfad/zur/neuen-datei>
```

<div id="migrate-mdx-endpoint-pages">
  ### MDX-Endpunktseiten migrieren
</div>

Migrieren Sie MDX-Endpunktseiten mit dem folgenden Befehl zu automatisch generierten Seiten aus Ihrer OpenAPI-Spezifikation:

```bash
mint migrate-mdx
```

Dieser Befehl konvertiert einzelne MDX-Endpunktseiten in automatisch generierte Seiten, die in Ihrer `docs.json` definiert sind, verschiebt MDX-Inhalte in die `x-mint`-Erweiterung Ihrer OpenAPI-Spezifikation und aktualisiert Ihre Navigation. Ausführliche Informationen finden Sie unter [Migration von MDX](/de/guides/migrating-from-mdx).

<div id="formatting">
  ## Formatierung
</div>

Für die lokale Entwicklung empfehlen wir Erweiterungen in Ihrer IDE, um `MDX`-Dateien zu erkennen und zu formatieren.

Wenn Sie Cursor, Windsurf oder VS Code verwenden, empfehlen wir die [MDX VS Code-Erweiterung](https://marketplace.visualstudio.com/items?itemName=unifiedjs.vscode-mdx) für Syntaxhervorhebung sowie [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode) für die Codeformatierung.

Wenn Sie JetBrains verwenden, empfehlen wir das [MDX IntelliJ-IDEA-Plugin](https://plugins.jetbrains.com/plugin/14944-mdx) für Syntaxhervorhebung und die Einrichtung von [Prettier](https://prettier.io/docs/webstorm) für die Codeformatierung.

<div id="troubleshooting">
  ## Fehlerbehebung
</div>

<AccordionGroup>
  <Accordion title="Fehler: Das Modul &#x22;sharp&#x22; konnte mit der darwin-arm64-Runtime nicht geladen werden">
    Dies kann an einer veralteten Version von Node liegen. Probiere Folgendes:

    1. Entferne die aktuell installierte Version der mint-CLI: `npm uninstall -g mint`
    2. Aktualisiere auf die neueste Version von Node.js.
    3. Installiere die mint-CLI erneut: `npm install -g mint`
  </Accordion>

  <Accordion title="Problem: Unbekannter Fehler tritt auf">
    **Lösung**: Wechsle ins Stammverzeichnis deines Geräts und lösche den Ordner `~/.mintlify`. Führe anschließend `mint dev` erneut aus.
  </Accordion>

  <Accordion title="Fehler: permission denied">
    Das liegt daran, dass die erforderlichen Berechtigungen für die globale Installation von Node-Paketen fehlen.

    **Lösung**: Führe `sudo npm i -g mint` aus. Du wirst nach deinem Passwort gefragt – das ist dasselbe, mit dem du deinen Computer entsperrst.
  </Accordion>

  <Accordion title="Die lokale Vorschau sieht nicht genauso aus wie meine Docs im Web">
    Das liegt wahrscheinlich an einer veralteten Version der CLI.

    **Lösung:** Führe `mint update` aus, um die neuesten Änderungen zu erhalten.
  </Accordion>

  <Accordion title="mintlify vs. mint package">
    Wenn du Probleme mit dem CLI-Paket hast, führe zuerst `npm ls -g` aus. Dieser Befehl zeigt, welche Pakete auf deinem Rechner global installiert sind.

    Wenn du npm nicht verwendest oder es nicht in der -g-Liste siehst, probiere `which mint`, um die Installation zu finden.

    Wenn sowohl ein Paket namens `mint` als auch eines namens `mintlify` installiert ist, solltest du `mintlify` deinstallieren.

    1. Deinstalliere das alte Paket:

    ```bash
      npm uninstall -g mintlify
    ```

    2. Leere deinen npm-Cache:

    ```bash
      npm cache clean --force
    ```

    3. Installiere das neue Paket:

    ```bash
    npm i -g mint
    ```
  </Accordion>
</AccordionGroup>