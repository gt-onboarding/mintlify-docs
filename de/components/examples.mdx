---
title: "Beispiele"
description: "Codeblöcke in der rechten Seitenleiste auf Desktop-Geräten anzeigen"
---

Die Komponenten `<RequestExample>` und `<ResponseExample>` zeigen Codeblöcke in der rechten Seitenleiste an, um ein zweispaltiges Layout zu schaffen, das Beispiele sichtbar hält, während Nutzer durch deine Inhalte scrollen. Diese Komponenten sind für API-Dokumentation konzipiert, funktionieren jedoch auf allen Seiten.

Häufige Anwendungsfälle:

* API-Endpunkt-Dokumentation mit Request- und Response-Beispielen
* Konfigurationsbeispiele neben erklärendem Text
* Codebeispiele, auf die sich Nutzer beim Befolgen von Anleitungen beziehen
* Vorher-/Nachher-Beispiele in Tutorials

Auf Mobilgeräten werden die Komponenten `<RequestExample>` und `<ResponseExample>` als normale Codeblöcke angezeigt und können überscrollt werden.

<RequestExample>
  ```bash Request
    curl --request POST \
      --url https://dog-api.kinduff.com/api/facts
  ```
</RequestExample>

<ResponseExample>
  ```json Response
  { "status": "success" }
  ```
</ResponseExample>

<div id="requestexample">
  ## RequestExample
</div>

Verwenden Sie `<RequestExample>`, um Codebeispiele in der rechten Seitenleiste anzupinnen. Diese Komponente funktioniert ähnlich wie [CodeGroup](/de/components/code-groups), zeigt den Code jedoch in der Seitenleiste statt inline an.

Sie können mehrere Codeblöcke in einem einzigen `<RequestExample>` einfügen. Jeder Codeblock muss ein title-Attribut besitzen.

````mdx RequestExample
<RequestExample>

```bash Request
  curl --request POST \
    --url https://dog-api.kinduff.com/api/facts
```

</RequestExample>
````

<div id="responseexample">
  ## ResponseExample
</div>

Die Komponente `<ResponseExample>` heftet Codebeispiele in der rechten Seitenleiste unterhalb sämtlicher `<RequestExample>`-Inhalte auf derselben Seite an.

````mdx ResponseExample
<ResponseExample>

```json Response
{ "status": "success" }
```

</ResponseExample>
````