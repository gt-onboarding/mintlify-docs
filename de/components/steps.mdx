---
title: "Schritte"
description: "Inhalte mit der Steps‑Komponente in eine Abfolge bringen"
icon: "list-todo"
---

import IconsOptional from "/snippets/de/icons-optional.mdx";

Verwenden Sie Steps, um eine Abfolge von aufeinanderfolgenden Aktionen oder Ereignissen darzustellen. Sie können beliebig viele Schritte hinzufügen.

<Steps>
  <Step title="First Step">
    Dies sind Anweisungen oder Inhalte, die nur für den ersten Schritt relevant sind.
  </Step>

  <Step title="Second Step">
    Dies sind Anweisungen oder Inhalte, die nur für den zweiten Schritt relevant sind.
  </Step>

  <Step title="Third Step">
    Dies sind Anweisungen oder Inhalte, die nur für den dritten Schritt relevant sind.
  </Step>
</Steps>

```mdx Steps example
<Steps>
  <Step title="First Step">
    These are instructions or content that only pertain to the first step.
  </Step>
  <Step title="Second Step">
    These are instructions or content that only pertain to the second step.
  </Step>
  <Step title="Third Step">
    These are instructions or content that only pertain to the third step.
  </Step>
</Steps>
```


<div id="steps-properties">
  ## Eigenschaften von Steps
</div>

<ResponseField name="children" type="ReactElement<StepProps>[]" required>
  Eine Liste von `Step`-Komponenten.
</ResponseField>

<ResponseField name="titleSize" type="string" default="p">
  Die Größe der Schrittüberschriften. Eine von `p`, `h2` und `h3`.
</ResponseField>

<div id="individual-step-properties">
  ## Individuelle Schritt-Eigenschaften
</div>

<ResponseField name="children" type="string | ReactNode" required>
  Der Inhalt eines Schritts, entweder als Klartext oder als Komponenten.
</ResponseField>

<IconsOptional />

<ResponseField name="title" type="string">
  Der Titel ist der Haupttext des Schritts und erscheint neben dem Indikator.
</ResponseField>

<ResponseField name="stepNumber" type="number">
  Die Nummer des Schritts.
</ResponseField>

<ResponseField name="titleSize" type="string" default="p">
  Die Größe der Schritttitel. Einer von `p`, `h2` und `h3`.
</ResponseField>