---
title: "Wiederverwendbare Snippets"
description: "Wiederverwendbare, benutzerdefinierte Snippets, um Inhalte konsistent zu halten"
icon: "recycle"
---

Eines der Grundprinzipien der Softwareentwicklung ist DRY („Don&#39;t Repeat Yourself“), und das gilt auch für Dokumentation. Wenn Sie feststellen, dass Sie denselben Inhalt an mehreren Stellen wiederholen, sollten Sie ein benutzerdefiniertes Snippet erstellen, um Ihre Inhalte konsistent zu halten.

<div id="creating-a-custom-snippet">
  ## Erstellen eines benutzerdefinierten Snippets
</div>

**Voraussetzung**: Legen Sie Ihre Snippet-Datei im Verzeichnis `snippets` an, damit der Import funktioniert.

Jede Seite im Verzeichnis `snippets` wird als Snippet behandelt und nicht
als eigenständige Seite gerendert. Wenn Sie aus dem Snippet eine eigenständige Seite
erstellen möchten, importieren Sie es in eine andere Datei und binden Sie es als
Komponente ein.

<div id="default-export">
  ### Standardexport
</div>

1. Fügen Sie Inhalte zu Ihrer Snippet-Datei hinzu, die Sie wiederverwenden möchten. Optional können Sie Variablen hinzufügen, die beim Import des Snippets über Props befüllt werden können. In diesem Beispiel heißt unsere Variable word.

```typescript snippets/my-snippet.mdx
Hello world! This is my content I want to reuse across pages. 
```

2. Importieren Sie das Snippet in Ihre Zieldatei.

```typescript destination-file.mdx
---
title: My title
description: My Description
---

import MySnippet from '/snippets/path/to/my-snippet.mdx';

## Überschrift

Lorem impsum dolor sit amet.

<MySnippet/>

```

<div id="exporting-with-variables">
  ### Exportieren mit Variablen
</div>

1. Optional können Sie Variablen hinzufügen, die beim Import des Snippets über Props befüllt werden können. In diesem Beispiel heißt unsere Variable word.

```typescript snippets/my-snippet.mdx
My keyword of the day is {word}.
```

2. Importieren Sie das Snippet mit der Variable in Ihre Zieldatei. Die Property wird gemäß Ihrer Angabe befüllt.

```typescript destination-file.mdx
---
title: My title
description: My Description
---

import MySnippet from '/snippets/path/to/my-snippet.mdx';

## Header

Lorem ipsum dolor sit amet.

<MySnippet word="bananas" />

```

<div id="reusable-variables">
  ### Wiederverwendbare Variablen
</div>

1. Exportiere eine Variable aus deiner Snippet-Datei:

```typescript snippets/path/to/custom-variables.mdx
export const myName = 'my name';

export const myObject = { fruit: 'strawberries' };
```

2. Importiere das Snippet in deiner Zieldatei und verwende die Variable:

```typescript destination-file.mdx
---
title: My title
description: My Description
---

import { myName, myObject } from '/snippets/path/to/custom-variables.mdx';

Hello, my name is {myName} and I like {myObject.fruit}.
```

<div id="jsx-snippets">
  ### JSX-Snippets
</div>

1. Exportieren Sie eine JSX-Komponente aus Ihrer Snippet-Datei. (Weitere Informationen finden Sie unter [React-Komponenten](/de/react-components)):

```js icon=square-js snippets/my-jsx-snippet.jsx
export const MyJSXSnippet = () => {
  return (
    <div>
      <h1>Hello, world!</h1>
    </div>
  )
}
```

<Note>
  Wichtig: Verwenden Sie beim Erstellen von JSX-Snippets die Pfeilfunktionssyntax (`=>`) statt Funktionsdeklarationen. Das Schlüsselwort `function` wird in diesem Kontext nicht unterstützt.
</Note>

2. Importieren Sie das Snippet in Ihrer Zieldatei und verwenden Sie die Komponente:

```typescript destination-file.mdx
---
title: My title
description: My Description
---

import { MyJSXSnippet } from '/snippets/my-jsx-snippet.jsx';

<MyJSXSnippet />
```