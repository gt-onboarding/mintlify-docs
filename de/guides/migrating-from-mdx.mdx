---
title: "Migration von MDX-API-Seiten zur OpenAPI-Navigation"
sidebarTitle: "Migration von MDX zu OAS"
description: "Umstieg von einzelnen MDX-Endpunktseiten auf automatisierte OpenAPI-Generierung mit flexibler Navigation"
---

Wenn Sie derzeit einzelne `MDX`-Seiten für Ihre API-Endpunkte verwenden, können Sie auf automatisch generierte Seiten aus Ihrer OpenAPI-Spezifikation umsteigen und dabei die Anpassbarkeit einzelner Seiten beibehalten. Das hilft, die Anzahl der zu pflegenden Dateien zu reduzieren und die Konsistenz Ihrer API-Dokumentation zu verbessern.

Sie können Metadaten und Inhalte für jeden Endpunkt in Ihrer OpenAPI-Spezifikation definieren und die Endpunkte in der Navigation an den gewünschten Stellen anordnen.

<div id="cli-migration">
  ## CLI-Migration
</div>

Der Befehl `mint migrate-mdx` ist der empfohlene Weg, um von MDX-Endpunktseiten zu automatisch generierten Seiten zu migrieren.

Dieser Befehl:

* Analysiert die Navigationsstruktur deiner `docs.json`.
* Identifiziert MDX-Seiten, die OpenAPI-Endpunktseiten erzeugen.
* Extrahiert Inhalte aus MDX-Dateien und verschiebt sie in die `x-mint`-Erweiterung deiner OpenAPI-Spezifikation.
* Aktualisiert deine `docs.json`, sodass direkt auf die OpenAPI-Endpunkte statt auf MDX-Dateien verwiesen wird.
* Löscht die ursprünglichen MDX-Endpunktdateien.

<Info>
  Wenn du bereits `x-mint` für einen Endpunkt definiert hast und außerdem eine MDX-Seite mit Inhalt für diesen Endpunkt vorhanden ist, überschreibt der MDX-Inhalt die bestehenden `x-mint`-Einstellungen.

  Wenn du mehrere MDX-Seiten für denselben Endpunkt mit unterschiedlichem Inhalt hast, verwendet das Skript den Inhalt der Seite, die in deiner `docs.json` zuletzt aufgeführt ist.

  Das Migrations-Tool unterstützt keine Vorschau der Änderungen, bevor sie angewendet werden.
</Info>

<Steps>
  <Step title="Bereite deine OpenAPI-Spezifikation vor.">
    Stelle sicher, dass deine OpenAPI-Spezifikation gültig ist und alle Endpunkte enthält, die du dokumentieren möchtest.

    Alle MDX-Seiten, die du migrieren möchtest, müssen im `openapi:`-Frontmatter auf einen Endpunkt verweisen.

    <Tip>
      Validiere deine OpenAPI-Datei mit dem [Swagger Editor](https://editor.swagger.io/) oder der [Mint CLI](https://www.npmjs.com/package/mint).
    </Tip>
  </Step>

  <Step title="Installiere die Mint CLI">
    Installiere oder aktualisiere bei Bedarf die [Mint CLI](/de/installation).
  </Step>

  <Step title="Führe den Migrationsbefehl aus.">
    ```bash
    mint migrate-mdx
    ```
  </Step>
</Steps>

<div id="manual-migration-steps">
  ## Manuelle Migrationsschritte
</div>

<Steps>
  <Step title="Bereiten Sie Ihre OpenAPI-Spezifikation vor.">
    Stellen Sie sicher, dass Ihre OpenAPI-Spezifikation gültig ist und alle Endpunkte enthält, die Sie dokumentieren möchten.

    Für Endpunkte, deren Metadaten oder Inhalte Sie anpassen möchten, fügen Sie die Erweiterung `x-mint` hinzu. Weitere Details finden Sie unter [x-mint extension](/de/api-playground/openapi-setup#x-mint-extension).

    Für Endpunkte, die Sie von Ihrer Dokumentation ausschließen möchten, fügen Sie die Erweiterung `x-hidden` hinzu.

    <Info>
      Validieren Sie Ihre OpenAPI-Datei mit dem [Swagger Editor](https://editor.swagger.io/) oder der [Mint CLI](https://www.npmjs.com/package/mint).
    </Info>
  </Step>

  <Step title="Aktualisieren Sie Ihre Navigationsstruktur.">
    Ersetzen Sie `MDX`-Seiten durch OpenAPI-Endpunkte in Ihrer `docs.json`.

    ```json
    "navigation": {
      "groups": [
        {
          "group": "API Reference",
          "openapi": "/path/to/openapi.json",
          "pages": [
            "overview",
            "authentication",
            "introduction",
            "GET /health",
            "quickstart", 
            "POST /users",
            "GET /users/{id}",
            "advanced-features"
          ]
        }
      ]
    }
    ```
  </Step>

  <Step title="Entfernen Sie alte MDX-Dateien.">
    Entfernen Sie, nachdem Sie überprüft haben, dass Ihre neue Navigation korrekt funktioniert, die `MDX`-Endpunktdateien, die Sie nicht mehr benötigen.
  </Step>
</Steps>

<div id="navigation-patterns">
  ## Navigationsmuster
</div>

Sie können anpassen, wie Ihre API-Dokumentation in der Navigation angezeigt wird.

<div id="mixed-content-navigation">
  ### Navigation für gemischte Inhalte
</div>

Kombinieren Sie automatisch generierte API-Seiten mit anderen Seiten:

```json
"navigation": {
  "groups": [
    {
      "group": "API-Referenz",
      "openapi": "openapi.json",
      "pages": [
        "api/overview",
        "GET /users",
        "POST /users", 
        "api/authentication"
      ]
    }
  ]
}
```

<div id="multiple-api-versions">
  ### Mehrere API-Versionen
</div>

Organisieren Sie verschiedene API-Versionen mit Tabs oder Gruppen:

```json
"navigation": {
  "tabs": [
    {
      "tab": "API v1",
      "openapi": "specs/v1.json"
    },
    {
      "tab": "API v2",
      "openapi": "specs/v2.json"
    }
  ]
}
```

<div id="when-to-use-individual-mdx-pages">
  ## Wann Sie individuelle `MDX`-Seiten verwenden sollten
</div>

Behalten Sie individuelle `MDX`-Seiten bei, wenn Sie Folgendes benötigen:

* Umfassende, pro Endpunkt maßgeschneiderte Inhalte wie React-Komponenten oder ausführliche Beispiele.
* Einzigartige Seitenlayouts.
* Experimentelle Dokumentationsansätze für bestimmte Endpunkte.

Für die meisten Anwendungsfälle bietet die OpenAPI-Navigation bessere Wartbarkeit und Konsistenz.