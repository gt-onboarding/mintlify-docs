---
title: "Paramètres globaux"
description: "Configurez les paramètres globaux du site avec le fichier `docs.json`"
icon: "settings-2"
keywords: ["docs.json", "paramètres", "personnalisation", "configuration"]
---

import IconsOptional from "/snippets/fr/icons-optional.mdx";

Le fichier `docs.json` vous permet de transformer une collection de fichiers Markdown en un site de documentation navigable et personnalisé. Ce fichier de configuration obligatoire contrôle le style, la navigation, les intégrations, et bien plus encore. Considérez-le comme le plan de votre documentation.

Les paramètres définis dans `docs.json` s’appliquent globalement à toutes les pages.

<div id="setting-up-your-docsjson">
  ## Configuration de votre `docs.json`
</div>

Pour démarrer, il suffit d’indiquer `theme`, `name`, `colors.primary` et `navigation`. Les autres champs sont facultatifs et vous pourrez les ajouter au fil de l’évolution de vos besoins en documentation.

Pour une meilleure expérience d’édition, ajoutez la référence de schéma en tête de votre fichier `docs.json`. Cela active l’autocomplétion, la validation et des info-bulles utiles dans la plupart des éditeurs de code :

```json
{
  "$schema": "https://mintlify.com/docs.json",
  "theme": "mint",
  "name": "Your Docs",
  "colors": {
    "primary": "#ff0000"
  },
  "navigation": {
    // Votre structure de navigation
  }
  // Le reste de votre configuration
}
```

<div id="reference">
  ## Référence
</div>

Cette section contient la référence complète du fichier `docs.json`.

<div id="customization">
  ### Personnalisation
</div>

<ResponseField name="theme" required>
  Le thème de mise en page de votre site.

  L’un des suivants : `mint`, `maple`, `palm`, `willow`, `linden`, `almond`, `aspen`.

  Voir les [Thèmes](fr/themes) pour en savoir plus.
</ResponseField>

<ResponseField name="name" type="string" required>
  Le nom de votre projet, de votre organisation ou de votre produit.
</ResponseField>

<ResponseField name="colors" type="object" required>
  Les couleurs utilisées dans votre documentation. Leur application varie selon les thèmes. Si vous ne fournissez qu’une couleur principale, elle sera utilisée pour tous les éléments colorés.

  <Expandable title="Couleurs">
    <ResponseField name="primary" type="string matching ^#([a-fA-F0-9]{6}|[a-fA-F0-9]{3})$" required>
      La couleur principale de votre documentation. Généralement utilisée pour la mise en avant en mode clair, avec quelques variations selon le thème.

      Doit être un code hexadécimal commençant par `#`.
    </ResponseField>

    <ResponseField name="light" type="string matching ^#([a-fA-F0-9]{6}|[a-fA-F0-9]{3})$">
      La couleur utilisée pour la mise en avant en mode sombre.

      Doit être un code hexadécimal commençant par `#`.
    </ResponseField>

    <ResponseField name="dark" type="string matching ^#([a-fA-F0-9]{6}|[a-fA-F0-9]{3})$">
      La couleur utilisée pour les boutons et les états de survol en modes clair et sombre, avec quelques variations selon le thème.

      Doit être un code hexadécimal commençant par `#`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  Description de votre site pour le référencement (SEO) et l’indexation par l’IA.
</ResponseField>

<ResponseField name="logo" type="string or object">
  Votre logo pour les modes clair et sombre.

  <Expandable title="Logo">
    <ResponseField name="light" type="string" required>
      Chemin vers le fichier de votre logo pour le mode clair. Incluez l’extension du fichier. Exemple : `/logo.png`
    </ResponseField>

    <ResponseField name="dark" type="string" required>
      Chemin vers le fichier de votre logo pour le mode sombre. Incluez l’extension du fichier. Exemple : `/logo-dark.png`
    </ResponseField>

    <ResponseField name="href" type="string (uri)">
      L’URL vers laquelle rediriger lors d’un clic sur le logo. Si elle n’est pas fournie, le logo renverra vers votre page d’accueil. Exemple : `https://mintlify.com`
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="favicon" type="string or object">
  Chemin vers votre fichier favicon, y compris l’extension. Redimensionné automatiquement aux tailles de favicon appropriées.
  Peut être un fichier unique ou des fichiers distincts pour les modes clair et sombre. Exemple : `/favicon.png`

  <Expandable title="Favicon">
    <ResponseField name="light" type="string" required>
      Chemin vers votre fichier favicon pour le mode clair. Incluez l’extension du fichier. Exemple : `/favicon.png`
    </ResponseField>

    <ResponseField name="dark" type="string" required>
      Chemin vers votre fichier favicon pour le mode sombre. Incluez l’extension du fichier. Exemple : `/favicon-dark.png`
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="thumbnails" type="object">
  Personnalisation des vignettes pour les réseaux sociaux et les aperçus de page.

  <Expandable title="Vignettes">
    <ResponseField name="appearance" type="&#x22;light&#x22; | &#x22;dark&#x22;">
      Le thème visuel de vos vignettes. S’il n’est pas précisé, elles utilisent le jeu de couleurs de votre site défini par le champ `colors`.
    </ResponseField>

    <ResponseField name="background" type="string">
      Image d’arrière-plan pour vos vignettes. Peut être un chemin relatif ou une URL absolue.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="styling" type="object">
  Configurations de style visuel.

  <Expandable title="Style">
    <ResponseField name="eyebrows" type="&#x22;section&#x22; | &#x22;breadcrumbs&#x22;">
      Le style de l’élément d’entête de page. Choisissez `section` pour afficher le nom de la section ou `breadcrumbs` pour afficher le fil d’Ariane complet. Par défaut : `section`.
    </ResponseField>

    <ResponseField name="codeblocks" type="&#x22;system&#x22; | &#x22;dark&#x22;">
      Le thème des blocs de code. Choisissez `system` pour faire correspondre le thème du site ou `dark` pour des blocs de code toujours sombres. Par défaut : `system`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="icons" type="object">
  Paramètres de la bibliothèque d’icônes.

  <Expandable title="Icônes">
    <ResponseField name="library" type="&#x22;fontawesome&#x22; | &#x22;lucide&#x22;" required>
      Bibliothèque d’icônes à utiliser dans l’ensemble de votre documentation. Par défaut : `fontawesome`.

      <Note>
        Vous pouvez spécifier l’URL d’une icône hébergée en externe, le chemin vers un fichier d’icône dans votre projet, ou du code SVG compatible JSX pour n’importe quelle icône, indépendamment du paramètre de bibliothèque.
      </Note>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fonts" type="object">
  Configuration des polices pour votre documentation. La police par défaut est [Inter](https://fonts.google.com/specimen/Inter).

  <Expandable title="Polices">
    <ResponseField name="family" type="string" required>
      Famille de polices, par exemple « Open Sans », « Playfair Display ».
    </ResponseField>

    <ResponseField name="weight" type="number">
      Graisse, par exemple 400 ou 700. Les polices variables prennent en charge des valeurs précises comme 550.
    </ResponseField>

    <ResponseField name="source" type="string (uri)">
      URL de la source de la police, par exemple https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2. Les [Google Fonts](https://fonts.google.com) sont chargées automatiquement lorsque vous indiquez un nom de `family` Google Font ; aucune URL source n’est donc nécessaire.
    </ResponseField>

    <ResponseField name="format" type="&#x22;woff&#x22; | &#x22;woff2&#x22;">
      Format du fichier de police.
    </ResponseField>

    <ResponseField name="heading" type="object">
      Remplace les paramètres de police spécifiquement pour les titres.

      <Expandable title="Titres">
        <ResponseField name="family" type="string" required>
          Famille de polices, par exemple « Open Sans », « Playfair Display ».
        </ResponseField>

        <ResponseField name="weight" type="number">
          Graisse, par exemple 400 ou 700. Les polices variables prennent en charge des valeurs précises comme 550.
        </ResponseField>

        <ResponseField name="source" type="string (uri)">
          URL de la source de la police, par exemple https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2. Les [Google Fonts](https://fonts.google.com) sont chargées automatiquement lorsque vous indiquez un nom de `family` Google Font ; aucune URL source n’est donc nécessaire.
        </ResponseField>

        <ResponseField name="format" type="&#x22;woff&#x22; | &#x22;woff2&#x22;">
          Format du fichier de police.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="body" type="object">
      Remplace les paramètres de police spécifiquement pour le corps du texte.

      <Expandable title="Corps">
        <ResponseField name="family" type="string" required>
          Famille de polices, par exemple « Open Sans », « Playfair Display ».
        </ResponseField>

        <ResponseField name="weight" type="number">
          Graisse, par exemple 400 ou 700. Les polices variables prennent en charge des valeurs précises comme 550.
        </ResponseField>

        <ResponseField name="source" type="string (uri)">
          URL de la source de la police, par exemple https://mintlify-assets.b-cdn.net/fonts/Hubot-Sans.woff2. Les [Google Fonts](https://fonts.google.com) sont chargées automatiquement lorsque vous indiquez un nom de `family` Google Font ; aucune URL source n’est donc nécessaire.
        </ResponseField>

        <ResponseField name="format" type="&#x22;woff&#x22; | &#x22;woff2&#x22;">
          Format du fichier de police.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="appearance" type="object">
  Paramètres de l’interrupteur de mode clair/sombre.

  <Expandable title="Apparence">
    <ResponseField name="default" type="&#x22;system&#x22; | &#x22;light&#x22; | &#x22;dark&#x22;">
      Mode de thème par défaut. Choisissez `system` pour refléter les réglages du système d’exploitation des utilisateurs, ou `light`/`dark` pour imposer un mode spécifique. Par défaut : `system`.
    </ResponseField>

    <ResponseField name="strict" type="boolean">
      Indique s’il faut masquer l’interrupteur clair/sombre. Par défaut : `true`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="background" type="object">
  Paramètres de couleur et de décoration de l’arrière-plan.

  <Expandable title="Arrière-plan">
    <ResponseField name="image" type="string or object">
      Image d’arrière-plan de votre site. Peut être un fichier unique ou des fichiers distincts pour les modes clair et sombre.

      <Expandable title="Image">
        <ResponseField name="light" type="string" required>
          Chemin vers votre image d’arrière-plan pour le mode clair. Incluez l’extension du fichier. Exemple : `/background.png`.
        </ResponseField>

        <ResponseField name="dark" type="string" required>
          Chemin vers votre image d’arrière-plan pour le mode sombre. Incluez l’extension du fichier. Exemple : `/background-dark.png`.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="decoration" type="&#x22;gradient&#x22; | &#x22;grid&#x22; | &#x22;windows&#x22;">
      Décoration d’arrière-plan pour votre thème.
    </ResponseField>

    <ResponseField name="color" type="object">
      Couleurs d’arrière-plan personnalisées pour les modes clair et sombre.

      <Expandable title="Couleur">
        <ResponseField name="light" type="string matching ^#([a-fA-F0-9]{6}|[a-fA-F0-9]{3})$">
          Couleur d’arrière-plan pour le mode clair.

          Doit être un code hexadécimal commençant par `#`.
        </ResponseField>

        <ResponseField name="dark" type="string matching ^#([a-fA-F0-9]{6}|[a-fA-F0-9]{3})$">
          Couleur d’arrière-plan pour le mode sombre.

          Doit être un code hexadécimal commençant par `#`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="structure">
  ### Structure
</div>

<ResponseField name="navbar" type="object">
  Éléments de la barre de navigation.

  <Expandable title="Navbar">
    <ResponseField name="links" type="array of object">
      Liens à afficher dans la barre de navigation.

      <Expandable title="Links">
        <ResponseField name="label" type="string" required>
          Texte du lien.
        </ResponseField>

        <ResponseField name="href" type="string (uri)" required>
          URL ou chemin de destination du lien.
        </ResponseField>

        <IconsOptional />
      </Expandable>
    </ResponseField>

    <ResponseField name="primary" type="object">
      Bouton principal de la barre de navigation.

      <Expandable title="Primary">
        <ResponseField name="type" type="&#x22;button&#x22; | &#x22;github&#x22;" required>
          Style du bouton. Choisissez `button` pour un bouton standard avec libellé, ou `github` pour un lien vers un dépôt GitHub avec icône.
        </ResponseField>

        <ResponseField name="label" type="string" required>
          Texte du bouton. S’applique uniquement lorsque `type` vaut `button`.
        </ResponseField>

        <ResponseField name="href" type="string (uri)" required>
          Destination du bouton. Doit être un chemin valide ou une URL externe. Si `type` est `github`, il doit s’agir de l’URL d’un dépôt GitHub.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="navigation" type="object" required>
  La structure de navigation de votre contenu.

  <Expandable title="Navigation">
    <ResponseField name="global" type="object">
      Éléments de navigation globaux affichés sur toutes les pages et sections.

      <Expandable title="Global">
        <ResponseField name="languages" type="array of object">
          Configuration du sélecteur de langue pour les sites localisés.

          <Expandable title="Langues">
            <ResponseField name="language" type="&#x22;en&#x22; | &#x22;cn&#x22; | &#x22;zh&#x22; | &#x22;zh-Hans&#x22; | &#x22;zh-Hant&#x22; | &#x22;es&#x22; | &#x22;fr&#x22; | &#x22;ja&#x22; | &#x22;jp&#x22; | &#x22;pt&#x22; | &#x22;pt-BR&#x22; | &#x22;de&#x22; | &#x22;ko&#x22; | &#x22;it&#x22; | &#x22;ru&#x22; | &#x22;id&#x22; | &#x22;ar&#x22; | &#x22;tr&#x22;" required>
              Code de langue au format ISO 639-1
            </ResponseField>

            <ResponseField name="default" type="boolean">
              Indique s’il s’agit de la langue par défaut.
            </ResponseField>

            <ResponseField name="hidden" type="boolean">
              Indique s’il faut masquer cette langue dans la sélection par défaut.
            </ResponseField>

            <ResponseField name="href" type="string (uri)" required>
              Chemin valide ou lien externe vers la version de la documentation dans cette langue.
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="versions" type="array of object">
          Configuration du sélecteur de version pour les sites multiversion.

          <Expandable title="Versions">
            <ResponseField name="version" type="string" required>
              Nom affiché de la version.

              Longueur minimale : 1
            </ResponseField>

            <ResponseField name="default" type="boolean">
              Indique s’il s’agit de la version par défaut.
            </ResponseField>

            <ResponseField name="hidden" type="boolean">
              Indique s’il faut masquer cette option de version par défaut.
            </ResponseField>

            <ResponseField name="href" type="string (uri)" required>
              URL ou chemin vers cette version de votre documentation.
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="tabs" type="array of object">
          Onglets de navigation de premier niveau pour organiser les sections principales.

          <Expandable title="Onglets">
            <ResponseField name="tab" type="string" required>
              Nom affiché de l’onglet.

              Longueur minimale : 1
            </ResponseField>

            <IconsOptional />

            <ResponseField name="hidden" type="boolean">
              Indique s’il faut masquer cet onglet par défaut.
            </ResponseField>

            <ResponseField name="href" type="string (uri)" required>
              URL ou chemin de destination de l’onglet.
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="anchors" type="array of object">
          Liens ancrés qui apparaissent en évidence dans la navigation de la barre latérale.

          <Expandable title="Ancres">
            <ResponseField name="anchor" type="string" required>
              Nom d’affichage de l’ancre.

              Longueur minimale : 1
            </ResponseField>

            <IconsOptional />

            <ResponseField name="color" type="object">
              Couleurs personnalisées pour l’ancre.

              <Expandable title="Couleur">
                <ResponseField name="light" type="string matching ^#([a-fA-F0-9]{6}|[a-fA-F0-9]{3})$">
                  Couleur de l’ancre en mode clair.

                  Doit être un code hexadécimal commençant par `#`.
                </ResponseField>

                <ResponseField name="dark" type="string matching ^#([a-fA-F0-9]{6}|[a-fA-F0-9]{3})$">
                  Couleur de l’ancre en mode sombre.

                  Doit être un code hexadécimal commençant par `#`.
                </ResponseField>
              </Expandable>
            </ResponseField>

            <ResponseField name="hidden" type="boolean">
              Indique s’il faut masquer cette ancre par défaut.
            </ResponseField>

            <ResponseField name="href" type="string (uri)" required>
              URL ou chemin de destination de l’ancre.
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="dropdowns" type="array of object">
          Menus déroulants pour organiser du contenu connexe.

          <Expandable title="Menus déroulants">
            <ResponseField name="dropdown" type="string" required>
              Nom d’affichage du menu déroulant.

              Longueur minimale : 1
            </ResponseField>

            <IconsOptional />

            <ResponseField name="hidden" type="boolean">
              Indique s’il faut masquer ce menu déroulant par défaut.
            </ResponseField>

            <ResponseField name="href" type="string (uri)" required>
              URL ou chemin de destination du menu déroulant.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="languages" type="array of object">
      Sélecteur de langue pour les sites [multilingues](fr/navigation#languages).
    </ResponseField>

    <ResponseField name="versions" type="array of object">
      Sélecteur de version pour les sites comportant plusieurs [versions](fr/navigation#versions).
    </ResponseField>

    <ResponseField name="tabs" type="array of object">
      Navigation de premier niveau : [onglets](fr/navigation#tabs).
    </ResponseField>

    <ResponseField name="anchors" type="array of object">
      [Ancres](fr/navigation#anchors) de la barre latérale.
    </ResponseField>

    <ResponseField name="dropdowns" type="array of object">
      [Menus déroulants](fr/navigation#dropdowns) pour regrouper du contenu associé.
    </ResponseField>

    <ResponseField name="groups" type="array of object">
      [Groupes](fr/navigation#groups) pour organiser le contenu en sections.
    </ResponseField>

    <ResponseField name="pages" type="array of string or object">
      Les [pages](fr/navigation#pages) qui composent votre documentation.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="interaction" type="object">
  Paramètres d’interaction utilisateur pour les éléments de navigation.

  <Expandable title="Interaction">
    <ResponseField name="drilldown" type="boolean">
      Contrôle le comportement de navigation automatique lors de la sélection de groupes de navigation. Définissez sur `true` pour forcer la navigation vers la première page lorsqu’un groupe de navigation est développé. Définissez sur `false` pour empêcher la navigation et uniquement développer ou réduire le groupe. Laissez non défini pour utiliser le comportement par défaut du thème.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="footer" type="object">
  Contenu du pied de page et liens vers les réseaux sociaux.

  <Expandable title="Footer">
    <ResponseField name="socials" type="object">
      Profils de réseaux sociaux à afficher dans le pied de page. Chaque clé est un nom de plateforme et chaque valeur est l’URL de votre profil. Par exemple :

      ```json
      {
        "x": "https://x.com/mintlify"
      }
      ```

      Noms de propriétés valides : `x`, `website`, `facebook`, `youtube`, `discord`, `slack`, `github`, `linkedin`, `instagram`, `hacker-news`, `medium`, `telegram`, `twitter`, `x-twitter`, `earth-americas`, `bluesky`, `threads`, `reddit`, `podcast`
    </ResponseField>

    <ResponseField name="links" type="array of object">
      Liens à afficher dans le pied de page.

      <Expandable title="Links">
        <ResponseField name="header" type="string">
          Titre de la colonne.

          Longueur minimale : 1
        </ResponseField>

        <ResponseField name="items" type="array of object" required>
          Liens à afficher dans la colonne.

          <Expandable title="Items">
            <ResponseField name="label" type="string" required>
              Texte du lien.

              Longueur minimale : 1
            </ResponseField>

            <ResponseField name="href" type="string (uri)" required>
              URL de destination du lien.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="banner" type="object">
  Bandeau global affiché en haut des pages.

  <Expandable title="Banner">
    <ResponseField name="content" type="string">
      Contenu du bandeau. Prend en charge le texte brut et le formatage Markdown. Par exemple :

      ```json
      {
        "content": "🚀 Le bandeau est en ligne ! [En savoir plus](mintlify.com)"
      }
      ```
    </ResponseField>

    <ResponseField name="dismissible" type="boolean">
      Indique si les utilisateurs peuvent masquer le bandeau. La valeur par défaut est `false`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="redirects" type="array of object">
  Redirections pour les pages déplacées, renommées ou supprimées.

  <Expandable title="Redirects">
    <ResponseField name="source" type="string" required>
      Chemin source à partir duquel rediriger. Exemple : `/old-page`
    </ResponseField>

    <ResponseField name="destination" type="string" required>
      Chemin de destination vers lequel rediriger. Exemple : `/new-page`
    </ResponseField>

    <ResponseField name="permanent" type="boolean">
      Indique s’il faut utiliser une redirection permanente (301). La valeur par défaut est `true`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="contextual" type="object">
  Menu contextuel pour le contenu optimisé par l’IA et les intégrations.

  <Expandable title="Contextual">
    <ResponseField name="options" type="array of &#x22;copy&#x22; | &#x22;view&#x22; | &#x22;chatgpt&#x22; | &#x22;claude&#x22;" required>
      Actions disponibles dans le menu contextuel. La première option apparaît par défaut.

      - `copy`: Copier la page actuelle en Markdown dans le presse-papiers.
      - `view`: Afficher la page actuelle en Markdown dans un nouvel onglet.
      - `chatgpt`: Envoyer le contenu de la page actuelle à ChatGPT.
      - `claude`: Envoyer le contenu de la page actuelle à Claude.

      <img src="/images/page-context-menu.png" alt="Menu contextuel" className="rounded-xl" />

      <Note>
        Le menu contextuel est disponible uniquement sur les déploiements de prévisualisation et de production.
      </Note>
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="api-configurations">
  ### Configurations de l’API
</div>

<ResponseField name="api" type="object">
  Paramètres de la documentation de l’API et du terrain de jeu interactif.

  <Expandable title="Api">
    <ResponseField name="openapi" type="string or array or object">
      Fichiers de spécification OpenAPI pour générer la documentation de l’API. Peut être une seule URL/un seul chemin ou un tableau d’URL/de chemins.

      <Expandable title="Openapi">
        <ResponseField name="source" type="string">
          URL ou chemin vers votre fichier de spécification OpenAPI.

          Longueur minimale : 1
        </ResponseField>

        <ResponseField name="directory" type="string">
          Répertoire dans lequel rechercher les fichiers OpenAPI.

          N’incluez pas de barre oblique initiale.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="asyncapi" type="string or array or object">
      Fichiers de spécification AsyncAPI pour générer la documentation de l’API. Peut être une seule URL/un seul chemin ou un tableau d’URL/de chemins.

      <Expandable title="Asyncapi">
        <ResponseField name="source" type="string">
          URL ou chemin vers votre fichier de spécification AsyncAPI.

          Longueur minimale : 1
        </ResponseField>

        <ResponseField name="directory" type="string">
          Répertoire dans lequel rechercher les fichiers AsyncAPI.

          N’incluez pas de barre oblique initiale.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="params" type="object">
      Paramètres d’affichage des paramètres de l’API.

      <Expandable title="Params">
        <ResponseField name="expanded" type="&#x22;all&#x22; | &#x22;closed&#x22;">
          Indique s’il faut développer tous les paramètres par défaut. Valeur par défaut : `closed`.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="playground" type="object">
      Paramètres du terrain de jeu de l’API.

      <Expandable title="Playground">
        <ResponseField name="display" type="&#x22;interactive&#x22; | &#x22;simple&#x22; | &#x22;none&#x22;">
          Mode d’affichage du terrain de jeu de l’API. Valeur par défaut : `interactive`.
        </ResponseField>

        <ResponseField name="proxy" type="boolean">
          Indique s’il faut faire transiter les requêtes API via un serveur proxy. Valeur par défaut : `true`.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="examples" type="object">
      Paramètres pour les exemples d’API générés automatiquement.

      <Expandable title="Examples">
        <ResponseField name="languages" type="array of string">
          Langues des exemples pour les extraits d’API générés automatiquement
        </ResponseField>

        <ResponseField name="defaults" type="&#x22;required&#x22; | &#x22;all&#x22;">
          Indique s’il faut afficher les paramètres optionnels dans les exemples d’API. Valeur par défaut : `all`.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="mdx" type="object">
      Paramètres pour les pages d’API générées à partir de fichiers `MDX`.

      <Expandable title="Mdx">
        <ResponseField name="auth" type="object">
          Configuration de l’authentification pour les requêtes d’API basées sur MDX.

          <Expandable title="Auth">
            <ResponseField name="method" type="&#x22;bearer&#x22; | &#x22;basic&#x22; | &#x22;key&#x22; | &#x22;cobo&#x22;">
              Méthode d’authentification pour les requêtes d’API.
            </ResponseField>

            <ResponseField name="name" type="string">
              Nom de l’authentification pour les requêtes d’API.
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="server" type="string or array">
          Configuration du serveur pour les requêtes d’API.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="seo-and-search">
  ### SEO et recherche
</div>

<ResponseField name="seo" type="object">
  Configurations d’indexation SEO.

  <Expandable title="Seo">
    <ResponseField name="metatags" type="object">
      Balises meta ajoutées à chaque page. Doit être une paire clé-valeur valide. Consultez la [référence des balises meta courantes](/fr/settings/seo#common-meta-tags-reference) pour les options.
    </ResponseField>

    <ResponseField name="indexing" type="&#x22;navigable&#x22; | &#x22;all&#x22;">
      Indiquez quelles pages les moteurs de recherche doivent indexer. Choisissez `navigable` pour indexer uniquement les pages présentes dans la navigation de votre `docs.json`, ou `all` pour indexer toutes les pages. Valeur par défaut : `navigable`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="search" type="object">
  Paramètres d’affichage de la recherche.

  <Expandable title="Search">
    <ResponseField name="prompt" type="string">
      Texte d’espace réservé à afficher dans la barre de recherche.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="integrations">
  ### Intégrations
</div>

<ResponseField name="integrations" type="object">
  Intégrations de tiers.

  <Expandable title="Integrations">
    <ResponseField name="amplitude" type="object">
      Intégration d’Analytics Amplitude.

      <Expandable title="Amplitude">
        <ResponseField name="apiKey" type="string" required>
          Votre clé API Amplitude.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="clearbit" type="object">
      Intégration d’enrichissement de données Clearbit.

      <Expandable title="Clearbit">
        <ResponseField name="publicApiKey" type="string" required>
          Votre clé d’API Clearbit.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="fathom" type="object">
      Intégration à Fathom Analytics.

      <Expandable title="Fathom">
        <ResponseField name="siteId" type="string" required>
          Votre identifiant de site Fathom.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="frontchat" type="object">
      Intégration du chat Front.

      <Expandable title="Frontchat">
        <ResponseField name="snippetId" type="string" required>
          L’identifiant de l’extrait du chat Front.

          Longueur minimale : 6
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="ga4" type="object">
      Intégration à Google Analytics 4.

      <Expandable title="GA4">
        <ResponseField name="measurementId" type="string matching ^G" required>
          Votre identifiant de mesure Google Analytics 4.

          Doit correspondre au motif : ^G
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="gtm" type="object">
      Intégration de Google Tag Manager.

      <Expandable title="Gtm">
        <ResponseField name="tagId" type="chaîne correspondant à ^G" required>
          Votre identifiant de balise Google Tag Manager.

          Doit correspondre au modèle : ^G
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="heap" type="object">
      Intégration d’Heap Analytics.

      <Expandable title="Heap">
        <ResponseField name="appId" type="string" required>
          Votre identifiant d’application Heap.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="hotjar" type="object">
      Intégration de Hotjar.

      <Expandable title="Hotjar">
        <ResponseField name="hjid" type="string" required>
          Votre identifiant Hotjar.
        </ResponseField>

        <ResponseField name="hjsv" type="string" required>
          Version de votre script Hotjar.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="intercom" type="object">
      Intégration à Intercom.

      <Expandable title="Intercom">
        <ResponseField name="appId" type="string" required>
          Votre ID d’application Intercom.

          Longueur minimale : 6
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="koala" type="object">
      Intégration à Koala.

      <Expandable title="Koala">
        <ResponseField name="publicApiKey" type="string" required>
          Votre clé API publique Koala.

          Longueur minimale : 2
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="logrocket" type="object">
      Intégration de LogRocket.

      <Expandable title="LogRocket">
        <ResponseField name="appId" type="string" required>
          L’ID de votre application LogRocket.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="mixpanel" type="object">
      Intégration à Mixpanel.

      <Expandable title="Mixpanel">
        <ResponseField name="projectToken" type="string" required>
          Votre jeton de projet Mixpanel.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="osano" type="object">
      Intégration à Osano.

      <Expandable title="Osano">
        <ResponseField name="scriptSource" type="string" required>
          La source de votre script Osano.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="pirsch" type="object">
      Intégration de l’outil d’analytics Pirsch.

      <Expandable title="Pirsch">
        <ResponseField name="id" type="string" required>
          Votre identifiant Pirsch.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="posthog" type="object">
      Intégration PostHog.

      <Expandable title="PostHog">
        <ResponseField name="apiKey" type="string matching ^phc\_" required>
          Votre clé API PostHog.

          Doit respecter le motif : ^phc\_
        </ResponseField>

        <ResponseField name="apiHost" type="string (uri)">
          Votre hôte d’API PostHog.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="plausible" type="object">
      Intégration de Plausible Analytics.

      <Expandable title="Plausible">
        <ResponseField name="domain" type="string" required>
          Votre domaine Plausible.
        </ResponseField>

        <ResponseField name="server" type="string">
          Votre serveur Plausible.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="segment" type="object">
      Intégration de Segment.

      <Expandable title="Segment">
        <ResponseField name="key" type="string" required>
          Votre clé Segment.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="telemetry" type="object">
      Paramètres de télémétrie.

      <Expandable title="Télémétrie">
        <ResponseField name="enabled" type="boolean">
          Indique si la télémétrie est activée.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="cookies" type="object">
      Paramètres des cookies.

      <Expandable title="Cookies">
        <ResponseField name="key" type="string">
          Clé du cookie.
        </ResponseField>

        <ResponseField name="value" type="string">
          Valeur du cookie.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="errors">
  ### Erreurs
</div>

<ResponseField name="errors" type="object">
  Paramètres de gestion des erreurs.

  <Expandable title="Erreurs">
    <ResponseField name="404" type="object">
      Gestion de l’erreur 404 « Page non trouvée ».

      <Expandable title="404">
        <ResponseField name="redirect" type="boolean">
          Indique s’il faut rediriger automatiquement vers la page d’accueil lorsqu’une page est introuvable.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="examples">
  ## Exemples
</div>

<Tabs>
  <Tab title="Basic example">
    ```json title="docs.json" wrap lines
    {
      "$schema": "https://mintlify.com/docs.json",
      "theme": "maple",
      "name": "Example Co.",
      "description": "Example Co. est une entreprise qui fournit du contenu d’exemple et du texte factice.",
      "colors": {
        "primary": "#3B82F6",
        "light": "#F8FAFC",
        "dark": "#0F172A"
      },
      "navigation": {
        "dropdowns": [
          {
            "dropdown": "Documentation",
            "icon": "book",
            "description": "Comment utiliser le produit Example Co.",
            "groups": [
              {
                "group": "Premiers pas",
                "pages": [
                  "index",
                  "quickstart"
                ]
              },
              {
                "group": "Personnalisation",
                "pages": [
                  "settings",
                  "users",
                  "features"
                ]
              },
              {
                "group": "Facturation",
                "pages": [
                  "billing/overview",
                  "billing/payments",
                  "billing/subscriptions"
                ]
              }
            ]
          },
          {
            "dropdown": "Journal des modifications",
            "icon": "history",
            "description": "Mises à jour et changements",
            "pages": [
              "changelog"
            ]
          }
        ]
      },
      "logo": {
        "light": "/logo-light.svg",
        "dark": "/logo-dark.svg",
        "href": "https://example.com"
      },
      "navbar": {
        "links": [
          {
            "label": "Communauté",
            "href": "https://example.com/community"
          }
        ],
        "primary": {
          "type": "button",
          "label": "Commencer",
          "href": "https://example.com/start"
        }
      },
      "footer": {
        "socials": {
          "x": "https://x.com/example",
          "linkedin": "https://www.linkedin.com/company/example",
          "github": "https://github.com/example",
          "slack": "https://example.com/community"
        },
        "links": [
          {
            "header": "Ressources",
            "items": [
              {
                "label": "Clients",
                "href": "https://example.com/customers"
              },
              {
                "label": "Entreprise",
                "href": "https://example.com/enterprise"
              },
              {
                "label": "Demander un aperçu",
                "href": "https://example.com/preview"
              }
            ]
          },
          {
            "header": "Société",
            "items": [
              {
                "label": "Carrières",
                "href": "https://example.com/careers"
              },
              {
                "label": "Blog",
                "href": "https://example.com/blog"
              },
              {
                "label": "Politique de confidentialité",
                "href": "https://example.com/legal/privacy"
              }
            ]
          }
        ]
      },
      "integrations": {
        "ga4": {
          "measurementId": "G-XXXXXXXXXX"
        },
        "koala": {
          "publicApiKey": "pk_example_key_123"
        },
        "telemetry": {
          "enabled": true
        },
        "cookies": {
          "key": "example_cookie_key",
          "value": "example_cookie_value"
        }
      },
      "contextual": {
        "options": [
          "copy",
          "view",
          "chatgpt",
          "claude"
        ]
      },
      "errors": {
        "404": {
          "redirect": true
        }
      }
    }
    ```
  </Tab>

  <Tab title="Interactive API example">
    ```json title="docs.json" wrap lines highlight={43-61, 72-79}
    {
      "$schema": "https://mintlify.com/docs.json",
      "theme": "maple",
      "name": "Example Co.",
      "description": "Example Co. est une entreprise qui fournit du contenu d'exemple et du texte factice.",
      "colors": {
        "primary": "#3B82F6",
        "light": "#F8FAFC",
        "dark": "#0F172A"
      },
      "navigation": {
        "dropdowns": [
          {
            "dropdown": "Documentation",
            "icon": "book",
            "description": "Guide d’utilisation du produit Example Co.",
            "groups": [
              {
                "group": "Prise en main",
                "pages": [
                  "index",
                  "quickstart"
                ]
              },
              {
                "group": "Personnalisation",
                "pages": [
                  "settings",
                  "users",
                  "features"
                ]
              },
              {
                "group": "Facturation",
                "pages": [
                  "billing/overview",
                  "billing/payments",
                  "billing/subscriptions"
                ]
              }
            ]
          },
          {
            "dropdown": "Référence API",
            "icon": "terminal",
            "description": "Guide d’utilisation de l’API Example Co.",
            "groups": [
              {
                "group": "Référence API",
                "pages": [
                  "api-reference/introduction"
                ]
              },
              {
                "group": "Points de terminaison",
                "openapi": {
                  "source": "openapi.json"
                }
              }
            ]
          },
          {
            "dropdown": "Journal des modifications",
            "icon": "history",
            "description": "Mises à jour et changements",
            "pages": [
              "changelog"
            ]
          }
        ]
      },
      "api": {
        "playground": {
          "display": "interactive"
        },
        "examples": {
          "languages": ["javascript", "curl", "python"]
        }
      },
      "logo": {
        "light": "/logo-light.svg",
        "dark": "/logo-dark.svg",
        "href": "https://example.com"
      },
      "navbar": {
        "links": [
          {
            "label": "Communauté",
            "href": "https://example.com/community"
          }
        ],
        "primary": {
          "type": "button",
          "label": "Commencer",
          "href": "https://example.com/start"
        }
      },
      "footer": {
        "socials": {
          "x": "https://x.com/example",
          "linkedin": "https://www.linkedin.com/company/example",
          "github": "https://github.com/example",
          "slack": "https://example.com/community"
        },
        "links": [
          {
            "header": "Ressources",
            "items": [
              {
                "label": "Clients",
                "href": "https://example.com/customers"
              },
              {
                "label": "Offre Entreprise",
                "href": "https://example.com/enterprise"
              },
              {
                "label": "Demander un aperçu",
                "href": "https://example.com/preview"
              }
            ]
          },
          {
            "header": "Entreprise",
            "items": [
              {
                "label": "Carrières",
                "href": "https://example.com/careers"
              },
              {
                "label": "Blog",
                "href": "https://example.com/blog"
              },
              {
                "label": "Politique de confidentialité",
                "href": "https://example.com/legal/privacy"
              }
            ]
          }
        ]
      },
      "integrations": {
        "ga4": {
          "measurementId": "G-XXXXXXXXXX"
        },
        "koala": {
          "publicApiKey": "pk_example_key_123"
        },
        "telemetry": {
          "enabled": true
        },
        "cookies": {
          "key": "example_cookie_key",
          "value": "example_cookie_value"
        }
      },
      "contextual": {
        "options": [
          "copy",
          "view",
          "chatgpt",
          "claude"
        ]
      },
      "errors": {
        "404": {
          "redirect": true
        }
      }
    }
    ```
  </Tab>

  <Tab title="Multi-language example">
    ```json title="docs.json" wrap lines
    {
      "$schema": "https://mintlify.com/docs.json",
      "theme": "maple",
      "name": "Example Co.",
      "description": "Example Co. est une entreprise qui propose du contenu d’exemple et du texte de substitution.",
      "colors": {
        "primary": "#3B82F6",
        "light": "#F8FAFC",
        "dark": "#0F172A"
      },
      "navigation": {
        "global": {
          "anchors": [
            {
              "anchor": "Documentation",
              "href": "https://mintlify.com/docs"
            },
            {
              "anchor": "Journal des modifications",
              "href": "https://mintlify.com/docs/changelog"
            }
          ]
        },
        "languages": [ // [!code highlight:3]
          {
            "language": "en",
            "dropdowns": [
              {
                "dropdown": "Documentation",
                "icon": "book",
                "description": "Comment utiliser le produit Example Co.",
                "pages": [
                  {
                    "group": "Prise en main",
                    "pages": ["index", "quickstart"]
                  },
                  {
                    "group": "Personnalisation",
                    "pages": ["settings", "users", "features"]
                  },
                  {
                    "group": "Facturation",
                    "pages": [
                      "billing/overview",
                      "billing/payments",
                      "billing/subscriptions"
                    ]
                  }
                ]
              },
              {
                "dropdown": "Journal des modifications",
                "icon": "history",
                "description": "Mises à jour et modifications",
                "pages": ["changelog"]
              }
            ]
          },
          {
            "language": "es",// [!code highlight]
            "dropdowns": [
              {
                "dropdown": "Documentación",
                "icon": "book",
                "description": "Cómo usar el producto de Example Co.",
                "pages": [
                  {
                    "group": "Comenzando",
                    "pages": ["es/index", "es/quickstart"]
                  },
                  {
                    "group": "Personalización",
                    "pages": ["es/settings", "es/users", "es/features"]
                  },
                  {
                    "group": "Billing",
                    "pages": [
                      "es/billing/overview",
                      "es/billing/payments",
                      "es/billing/subscriptions"
                    ]
                  }
                ]
              },
              {
                "dropdown": "Changelog",
                "icon": "history",
                "description": "Actualizaciones y cambios",
                "pages": ["es/changelog"]
              }
            ]
          }
        ]
      },
      "logo": {
        "light": "/logo-light.svg",
        "dark": "/logo-dark.svg",
        "href": "https://example.com"
      },
      "navbar": {
        "links": [
          {
            "label": "Communauté",
            "href": "https://example.com/community"
          }
        ],
        "primary": {
          "type": "button",
          "label": "Commencer",
          "href": "https://example.com/start"
        }
      },
      "footer": {
        "socials": {
          "x": "https://x.com/example",
          "linkedin": "https://www.linkedin.com/company/example",
          "github": "https://github.com/example",
          "slack": "https://example.com/community"
        },
        "links": [
          {
            "header": "Ressources",
            "items": [
              {
                "label": "Clients",
                "href": "https://example.com/customers"
              },
              {
                "label": "Offre Entreprise",
                "href": "https://example.com/enterprise"
              },
              {
                "label": "Demander une préversion",
                "href": "https://example.com/preview"
              }
            ]
          },
          {
            "header": "Entreprise",
            "items": [
              {
                "label": "Carrières",
                "href": "https://example.com/careers"
              },
              {
                "label": "Blog",
                "href": "https://example.com/blog"
              },
              {
                "label": "Politique de confidentialité"
                "href": "https://example.com/legal/privacy"
              }
            ]
          }
        ]
      },
      "integrations": {
        "ga4": {
          "measurementId": "G-XXXXXXXXXX"
        },
        "koala": {
          "publicApiKey": "pk_example_key_123"
        },
        "telemetry": {
          "enabled": true
        },
        "cookies": {
          "key": "example_cookie_key",
          "value": "example_cookie_value"
        }
      },
      "contextual": {
        "options": ["copy", "view", "chatgpt", "claude"]
      },
      "errors": {
        "404": {
          "redirect": true
        }
      }
    }
    ```
  </Tab>
</Tabs>

<div id="upgrading-from-mintjson">
  ## Mise à niveau depuis `mint.json`
</div>

Si votre projet de documentation utilise le fichier obsolète `mint.json`, suivez ces étapes pour migrer vers `docs.json`.

<Steps>
  <Step title="Installer ou mettre à jour le CLI">
    Si vous n'avez pas installé le [CLI](/fr/installation), installez-le maintenant :

    <CodeGroup>
      ```bash npm
      npm i -g mint
      ```

      ```bash yarn
      yarn global add mint
      ```

      ```bash pnpm
      pnpm add -g mint
      ```
    </CodeGroup>

    Si le CLI est déjà installé, assurez-vous qu’il est à jour :

    ```bash
    mint update
    ```
  </Step>

  <Step title="Créer votre fichier docs.json">
    Dans votre dépôt de documentation, exécutez :

    ```bash
    mint upgrade
    ```

    Cette commande créera un fichier `docs.json` à partir de votre `mint.json` existant. Passez en revue le fichier généré pour vérifier que tous les paramètres sont corrects.
  </Step>

  <Step title="Supprimer votre fichier mint.json">
    Après avoir confirmé que votre `docs.json` est correctement configuré, vous pouvez supprimer en toute sécurité votre ancien fichier `mint.json`.
  </Step>
</Steps>