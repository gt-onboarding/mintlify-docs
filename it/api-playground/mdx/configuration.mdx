---
title: "Configurazione MDX"
description: "Genera pagine di documentazione per gli endpoint della tua API utilizzando `MDX`"
---

Puoi definire manualmente gli endpoint dell’API in singoli file `MDX` invece di utilizzare una specifica OpenAPI. Questo metodo offre flessibilità per contenuti personalizzati, ma per la maggior parte dei progetti consigliamo di generare la documentazione API da un file di specifica OpenAPI, perché più manutenibile e ricco di funzionalità. Tuttavia, creare pagine `MDX` per un’API può essere utile per documentare API di piccole dimensioni o per prototipare.

Per generare pagine per gli endpoint API utilizzando `MDX`, configura le impostazioni dell’API in `docs.json`, crea singoli file `MDX` per ciascun endpoint e utilizza componenti come `<ParamFields />` per definire i parametri. A partire da queste definizioni, Mintlify genera API playground interattivi, esempi di richieste ed esempi di risposte.

<Steps>
  <Step title="Configura la tua API">
    Nel file `docs.json`, definisci l’URL di base e il metodo di autenticazione:

    ```json
     "api": {
      "mdx": {
        "server": "https://mintlify.com/api", // string array per più URL di base
        "auth": {
          "method": "key",
          "name": "x-api-key" // opzioni: bearer, basic, key.
        }
      }
    }
    ```

    Se vuoi nascondere l’API playground, usa il campo `display`. Non è necessario includere un metodo di autenticazione se nascondi il playground.

    ```json
    "api": {
      "playground": {
        "display": "none"
      }
    }
    ```

    Trovi l’elenco completo delle configurazioni API in [Settings](/it/settings#api-configurations).
  </Step>

  <Step title="Crea le pagine dei tuoi endpoint">
    Ogni pagina di endpoint API deve avere un corrispondente file `MDX`. All’inizio di ogni file, definisci `title` e `api`:

    ```mdx
    ---
    title: 'Create new user'
    api: 'POST https://api.mintlify.com/user'
    ---
    ```

    Puoi specificare i parametri di percorso aggiungendo il nome del parametro al path, racchiuso tra `{}`:

    ```bash
    https://api.example.com/v1/endpoint/{userId}
    ```

    <Note>
      Se hai un campo `server` configurato in `docs.json`, puoi usare percorsi relativi come `/v1/endpoint`.
    </Note>

    Puoi eseguire l’override, per una singola pagina, della modalità di visualizzazione dell’API playground definita a livello globale aggiungendo `playground` al frontmatter:

    ```mdx
    ---
    title: 'Create new user'
    api: 'POST https://api.mintlify.com/user'
    playground: 'none'
    ---
    ```

    * `playground: 'interactive'` - Mostra il playground interattivo.
    * `playground: 'simple'` - Mostra un endpoint copiabile senza playground.
    * `playground: 'none'` - Nasconde il playground.
  </Step>

  <Step title="Aggiungi gli endpoint alla documentazione">
    Aggiungi le pagine degli endpoint alla sidebar includendo i percorsi nel campo `navigation` del file `docs.json`. Scopri di più su come strutturare la documentazione in [Navigation](/it/navigation).
  </Step>
</Steps>

## Abilitare l&#39;autenticazione

Puoi aggiungere un metodo di autenticazione a `docs.json` per abilitarlo globalmente su tutte le pagine, oppure puoi impostarlo pagina per pagina.

Il metodo di autenticazione definito su una pagina ha la precedenza su quello globale se entrambi sono configurati.

<div id="bearer-token">
  ### Token bearer
</div>

<CodeGroup>
  ```json docs.json
  "api": {
      "mdx": {
        "auth": {
          "method": "bearer"
        }
      }
  }
  ```

  ```mdx Page Metadata
  ---
  title: "Titolo della pagina"
  authMethod: "bearer"
  ---
  ```
</CodeGroup>

<div id="basic-authentication">
  ### Autenticazione Basic
</div>

<CodeGroup>
  ```json docs.json
  "api": {
      "mdx": {
        "auth": {
          "method": "basic"
        }
      }
  }
  ```

  ```mdx Page Metadata
  ---
  title: "Titolo della pagina"
  authMethod: "basic"
  ---
  ```
</CodeGroup>

<div id="api-key">
  ### Chiave API
</div>

<CodeGroup>
  ```json docs.json
  "api": {
      "mdx": {
        "auth": {
          "method": "key",
          "name": "x-api-key"
        }
      }
  }
  ```

  ```mdx Metadati pagina
  ---
  title: "Titolo della pagina"
  authMethod: "key"
  ---
  ```
</CodeGroup>

<div id="none">
  ### None
</div>

Il metodo di autenticazione `none` è utile per disabilitare l&#39;autenticazione su un endpoint specifico dopo aver definito un valore predefinito in docs.json.

<CodeGroup>
  ```mdx Page Metadata
  ---
  title: "Your page title"
  authMethod: "none"
  ---
  ```
</CodeGroup>