---
title: "Icone"
description: "Usa le icone delle principali librerie"
icon: "flag"
---

import IconsRequired from "/snippets/it/icons-required.mdx";

Usa icone da Font Awesome, Lucide, SVG, URL esterni o file nel tuo progetto per arricchire la documentazione.

<Icon icon="flag" size={32} />

```mdx Icon example
<Icon icon="flag" size={32} />
```


<div id="inline-icons">
  ## Icone inline
</div>

Le icone vengono posizionate inline quando sono usate all'interno di un paragrafo.

<Icon icon="flag" iconType="solid" /> La documentazione che vuoi, senza sforzo.

```markdown Inline icon example
<Icon icon="flag" iconType="solid" /> La documentazione che vuoi, senza sforzo.
```

<div id="properties">
  ## Proprietà
</div>

<IconsRequired />

<ResponseField name="color" type="string">
  Il colore dell'icona in formato esadecimale (ad esempio `#FF5733`).
</ResponseField>

<ResponseField name="size" type="number">
  La dimensione dell'icona in pixel.
</ResponseField>