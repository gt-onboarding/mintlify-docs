---
title: "Sezioni espandibili"
description: "Attiva/disattiva per visualizzare proprietà annidate"
icon: "list-tree"
---

Usa le sezioni espandibili per mostrare e nascondere contenuti annidati all'interno dei campi di risposta. Sono particolarmente utili per visualizzare le proprietà di oggetti complessi nella documentazione API.

<ResponseField name="user" type="User Object">
  <Expandable title="properties">

<ResponseField name="full_name" type="string">
  Il nome completo dell’utente
</ResponseField>

<ResponseField name="is_over_21" type="boolean">
  Indica se l’utente ha più di 21 anni
</ResponseField>

  </Expandable>
</ResponseField>

```mdx Expandable example
<ResponseField name="user" type="User Object">
  <Expandable title="properties">
    <ResponseField name="full_name" type="string">
      Il nome completo dell’utente
    </ResponseField>

    <ResponseField name="is_over_21" type="boolean">
      Indica se l’utente ha più di 21 anni
    </ResponseField>
  </Expandable>
</ResponseField>
```

<div id="properties">
  ## Proprietà
</div>

<ResponseField name="title" type="string">
  Il nome dell'oggetto visualizzato.
</ResponseField>

<ResponseField name="defaultOpen" type="boolean" default="false">
  Imposta su `true` per aprire il riquadro espandibile all'apertura della pagina.
</ResponseField>