---
title: "Mixpanel"
---

Aggiungi quanto segue al file `docs.json` per inviare dati di analytics a Mixpanel.

```json Analytics options in docs.json
"integrations": {
    "mixpanel": {
        "projectToken": "required"
    }
}
```