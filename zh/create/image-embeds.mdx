---
title: "图片与嵌入"
description: "添加图片、视频和 iframe"
---

在文档中添加图片、嵌入视频，并通过 iframe 引入交互式内容。

<Frame>
  <img className="rounded-xl" src="https://mintlify-assets.b-cdn.net/bigbend.jpg" alt="一张风景照：前景是紫色花丛，远处群山连绵，湛蓝天空点缀着疏散的云朵。" />
</Frame>

<div id="images">
  ## 图片
</div>

在文档中添加图片，以提供可视化的上下文、示例或装饰。

<div id="basic-image-syntax">
  ### 基础图片语法
</div>

使用[Markdown 语法](https://www.markdownguide.org/basic-syntax/#images)在文档中添加图片：

```mdx
![描述图片的替代文本](/path/to/image.png)
```

<Tip>
  请务必添加具有描述性的 alt 文本，以提升可访问性和 SEO。alt 文本应清晰说明图像所呈现的内容。
</Tip>

图像文件必须小于 20 MB。对于更大的文件，请将其托管在 [Amazon S3](https://aws.amazon.com/s3) 或 [Cloudinary](https://cloudinary.com) 等 CDN 服务上。

<div id="html-image-embeds">
  ### 使用 HTML 嵌入图像
</div>

如需更精确地控制图像的显示，请使用 HTML `<img>` 标签：

```jsx
<img 
  src="/images/dashboard.png" 
  alt="主控制台界面"
  style={{height: "300px", width: "400px"}}
  className="rounded-lg"
/>
```

<div id="resize-images-with-inline-styles">
  #### 使用内联样式调整图像大小
</div>

使用 `style` 属性在 JSX 中添加内联样式来调整图像大小：

```jsx
<img
  src="/images/architecture.png"
  style={{width: "450px", height: "auto"}}
  alt="系统架构图"
/>
```

<div id="disable-zoom-functionality">
  #### 禁用缩放功能
</div>

要禁用图片点击时的默认缩放行为，请添加 `noZoom` 属性：

```html highlight="4"
<img 
  src="/images/screenshot.png" 
  alt="描述性替代文本"
  noZoom
/>
```

<div id="link-images">
  #### 链接图像
</div>

要将图像变为可点击的链接，请将其包裹在锚点标签中，并添加 `noZoom` 属性：

```html
<a href="https://mintlify.com" target="_blank">
  <img 
    src="/images/logo.png" 
    alt="Mintlify 徽标"
    noZoom
  />
</a>
```

<Note>
  放在链接标签内的图片会自动显示指针光标，表示它们可被点击。
</Note>

<div id="light-and-dark-mode-images">
  #### 明暗模式下的图片
</div>

要在浅色和深色主题中显示不同的图片，请使用 Tailwind CSS 类：

```html
<!-- 浅色模式图片 -->
<img 
  className="block dark:hidden" 
  src="/images/light-mode.png" 
  alt="浅色模式界面"
/>

<!-- 深色模式图片 -->
<img 
  className="hidden dark:block" 
  src="/images/dark-mode.png" 
  alt="深色模式界面"
/>
```

<div id="videos">
  ## 视频
</div>

Mintlify 支持在 Markdown 中使用 [HTML 标签](https://www.markdownguide.org/basic-syntax/#html)，让你可以灵活创建更丰富的内容。

<Tip>
  请务必在视频元素中加入备用文本内容，以便在不支持视频播放的浏览器中显示替代信息。
</Tip>

<div id="youtube-embeds">
  ### 嵌入 YouTube
</div>

使用 iframe 元素嵌入 YouTube 视频：

```html
<iframe
  className="w-full aspect-video rounded-xl"
  src="https://www.youtube.com/embed/4KzFe50RQkQ"
  title="YouTube 视频播放器"
  frameBorder="0"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
  allowFullScreen
></iframe>
```

<Frame>
  <iframe className="w-full aspect-video rounded-xl" src="https://www.youtube.com/embed/4KzFe50RQkQ" title="YouTube 视频播放器" frameBorder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen />
</Frame>

<div id="self-hosted-videos">
  ### 自托管视频
</div>

对于自托管的视频内容，请使用 HTML `<video>` 元素：

```html
<video
  controls
  className="w-full aspect-video rounded-xl"
  src="link-to-your-video.com"
></video>
```

<div id="autoplay-videos">
  ### 自动播放视频
</div>

要使视频自动播放，请使用：

```html
<video
  autoPlay
  muted
  loop
  playsInline
  className="w-full aspect-video rounded-xl"
  src="/videos/demo.mp4"
></video>
```

<Note>
  使用 JSX 语法时，将由两个单词组成的属性写为驼峰命名：`autoPlay`、`playsInline`、`allowFullScreen`。
</Note>

<div id="iframes">
  ## iframes
</div>

使用 iframe 元素嵌入外部内容：

```html
<iframe 
  src="https://example.com/embed" 
  title="嵌入的内容"
  className="w-full h-96 rounded-xl"
></iframe>
```

<div id="related-resources">
  ## 相关资源
</div>

<Card title="Frame 组件参考" icon="frame" horizontal href="/zh/components/frames">
  了解如何使用 Frame 组件呈现图像。
</Card>