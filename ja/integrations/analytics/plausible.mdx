---
title: "Plausible"
---

Plausible に分析データを送信するには、サイトのドメインを `docs.json` に追加します。

<Info>

    ドメインやサーバーには `https://` を含めないでください。

</Info>

<CodeGroup>

```json Analytics options in docs.json
"integrations": {
    "plausible": {
        "domain": "required",
        "server": "optional"
    }
}
```

```json Example
"integrations": {
    "plausible": {
        "domain": "docs.domain.com"
    }
}
```

</CodeGroup>