---
title: "バナー"
description: "サイト全体で重要なお知らせや通知を表示するバナーを追加する"
icon: "layout-panel-top"
---

バナーを使うと、ドキュメントサイト全体に重要なお知らせ、更新情報、通知を表示できます。バナーは各ページの上部に表示され、Markdownの書式に対応し、ユーザーが閉じられるように設定できます。

バナーを追加するには、`docs.json` の `banner` プロパティを使用します:

<CodeGroup>
  ```json Product announcements wrap
  "banner": {
    "content": "🚀 Version 2.0 is now live! See our [changelog](/changelog) for details.",
    "dismissible": true 
  }
  ```

  ```json Maintenance notices wrap
  "banner": {
    "content": "⚠️ Scheduled maintenance: API will be unavailable December 15, 2-4 AM UTC",
    "dismissible": false
  }
  ```

  ```json Required actions wrap
  "banner": {
    "content": "**Action required:** Migrate to our new version by January 1. [Migration guide](/migration)",
    "dismissible": true
  }
  ```
</CodeGroup>

<div id="properties">
  ## プロパティ
</div>

<ResponseField name="content" type="string" required>
  バナーのメッセージ。プレーンテキストとMarkdownの書式に対応します。
</ResponseField>

<ResponseField name="dismissible" type="boolean">
  ユーザーがバナーを閉じられるかどうか。`true` の場合、ユーザーはバナーを閉じることができ、同一セッション内では再表示されません。デフォルトは `false` です。
</ResponseField>